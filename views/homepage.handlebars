<div class="d-flex justify-content-center align-items-center">    
    <div class="signup-card home-card">
        {{#if loggedIn}}
        <BR>
        <h2 class="page-title login-margin">Hi {{userData.first_name}} {{userData.last_name}}</h2>
        {{/if}}
        <br><h4 class="page-title login-margin">What Would you Like to do?</h3>
        <div class="form-group d-flex flex-column">
            <a class="homeButton" href="/manage">Manage My Dogs</a>
            <a class="homeButton" href="/book">Book a Service</a>
            <a class="homeButton" href="/appointments">View my Bookings</a>
            <a class="homeButton" href="/contact">Contact Us</a>
            <a class="homeButton" id="logout2" href="/login">Logout</a>            
        </div>
    </div>
</div> 
<script src="/js/logout.js"></script>