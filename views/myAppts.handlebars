{{#each bookingData as |booking|}}
  <div class='d-flex justify-content-center'>
    <div class='signup-card'>
      <h2>Booking # {{booking.id}}</h2>
      <h2>{{booking.session_name}}</h2>
      <h2>{{format_date booking.session_datetime}}</h2>
    </div></div>
{{/each}}